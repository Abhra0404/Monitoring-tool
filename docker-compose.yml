version: "3.8"

services:
  theoria:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-4000}:4000"
    environment:
      - PORT=4000
      - JWT_SECRET=${JWT_SECRET:-changeme_generate_a_real_secret}
    volumes:
      - theoria-data:/root/.theoria

volumes:
  theoria-data:
